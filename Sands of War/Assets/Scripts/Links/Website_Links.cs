using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Website_Links : MonoBehaviour
{
    // Start is called before the first frame update
    public void Eden_Links(string Web_link)
    {
        Application.OpenURL(Web_link);
    }


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
